@model FCAI.BL.Model.InstractorVM
@inject FCAI.BL.Interface.IDepartmentRep deptRep
@{
    ViewData["Title"] = SharedLocalizer["Edit"];
    ViewBag.inst = "active";

}

<div class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1 class="m-0">@ViewData["Title"]</h1>
            </div><!-- /.col -->
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">@SharedLocalizer["Home"]</a></li>
                    <li class="breadcrumb-item"><a asp-controller="Student" asp-action="Index">@SharedLocalizer["Instractor"]</a></li>
                    <li class="breadcrumb-item active">@SharedLocalizer["Edit"]</li>
                </ol>
            </div><!-- /.col -->
        </div><!-- /.row -->
    </div><!-- /.container-fluid -->
</div>

<!-- Main content -->
<section class="content">
    <div class="container-fluid">
    <div class="text-danger">@ViewBag.x</div>
        <form asp-controller="Instractor" asp-action="Edit" class="w-75 mx-auto">
            <div class="form-group">
                <label asp-for="Name">@SharedLocalizer["Name"] :</label>
                <input asp-for="Name" class="form-control" type="text" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Salary">@SharedLocalizer["Salary"] :</label>
                <input asp-for="Salary" class="form-control" type="text" />
                <span asp-validation-for="Salary" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Phone">@SharedLocalizer["Phone"] :</label>
                <input asp-for="Phone" class="form-control" type="text" />
                <span asp-validation-for="Phone" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Email">@SharedLocalizer["Email"] :</label>
                <input asp-for="Email" class="form-control" type="text" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="DeptId">@SharedLocalizer["Department"] :</label>
                <select asp-for="DeptId" class="form-control">
                    <option>@SharedLocalizer["Select Department"]</option>
                    @foreach (var item in deptRep.Get())
                    {
                        <option value="@item.Id">@item.Name</option>
                    }
                </select>
                <span asp-validation-for="DeptId" class="text-danger"></span>
            </div>
            <div class="text-center">
                <button type="submit" class="btn btn-outline-success">@SharedLocalizer["Save"]</button>
                <button type="reset" class="btn btn-outline-danger">@SharedLocalizer["Cancel"]</button>
                <a asp-controller="Instractor" asp-action="Index" class="btn btn-outline-info">@SharedLocalizer["Back"]</a>
            </div>
        </form>
    </div>
</section>

@section Valid{
    <script src="~/plugins/jquery-validation/jquery.validate.min.js"></script>
    <script src="~/jquery-validation-unobtrusive/jquery.validate.unobtrusive.min.js"></script>
}

